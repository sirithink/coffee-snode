.tbox
    .rssbook.light(style="width:auto")
        p.info 填写您的邮件地址:
        .mailInput
            form(action="http://list.qq.com/cgi-bin/qf_compose_send", target="_blank", method="post")
                input(type="hidden", name="t", value="qf_booked_feedback")
                input(type="hidden", name="id", value="fbb30074540693f33c4c8ef64765c6cd79d6957e68d752c5")
                input#to.rsstxt(name="to", type="text", value="")
                .rssbutton
                    input(type="submit", value="订阅")
.tbox
    .header
        span.col_fade 近期文章
    .content
         ul.unstyled
            - blogs.forEach(function(item){
                li
                    p
                        - var title = item.title, reLen = 0; 
                        - for (var i = 0, len = title.length; i < len; i++) {
                                - if(reLen > 22) {
                                -       title = title.substr(0, i) + '...'
                                -       break
                                - }      
                                - if (title.charCodeAt(i) < 27 || title.charCodeAt(i) > 126){
                                -       reLen += 2        
                                - } else {
                                -       reLen++ 
                                - }        
                        - } 
                        a(href="/blog/#{item.id}", title="#{title}") #{title}
            - })