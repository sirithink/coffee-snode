extends common/_layout
block navbar
    include common/_navbar
block content
    .page-header
        h1 Snode
            small.pull-right ——记录在nodejs学习过程中踩过的那些坑！
    
    .row
        .span9
           - blogs.forEach(function(item){
            .media
                .media-body
                    h4.media-heading
                        a(href="/blog/#{item.id}", title="#{item.title}") #{item.title}
                    |#{item.update_time}
           - })
            include common/_pager
        .span3
            .tbox
                script(charset="UTF-8")
                    var nId = "fbb30074540693f33c4c8ef64765c6cd79d6957e68d752c5",nWidth="auto",sColor="light",sText="填写您的邮件地址:" ;
                script(src="http://list.qq.com/zh_CN/htmledition/js/qf/page/qfcode.js", charset="gb18030")
            .tbox
                .header
                     span.col_fade 近期文章
                .content
                     ul.unstyled
                        - blogs.forEach(function(item){
                            li
                                p
                                    a(href="/blog/#{item.id}", title="#{item.title}") #{item.title}
                        - })
                                
block script
    script(charset="UTF-8")